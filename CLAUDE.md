# CLAUDE.md - AI 助手行为宪章

⚠️  **极其重要：你作为本项目的 AI 开发助手，每次对话开始前，必须严格遵守本文件定义的所有规则。违反这些规则会导致开发偏离项目方向。

---

## 一、每次对话必须优先阅读的文档

在开始任何工作之前，你必须先阅读以下文档：

### 通用文档
1. **`docs/10-宪章/product-vision-v2.0.md`** - 产品愿景与定位，所有决策的最高依据
2. **`docs/30-服务架构/01-高层设计/02-演进路线图-v2.0.md`** - 架构演进路线图
3. **`docs/10-宪章/开发流程规范.md`** - TDD 流程、PR 流程、发布流程
4. **`docs/10-宪章/安全红线.md`** - 必须遵守的安全原则
5. **`docs/10-宪章/git-commit规范.md`** - Git 提交规范

### 后端开发（Go）
- **`docs/10-宪章/代码风格规范.md`** - Go 代码风格

### 前端开发（Vue 3）
⚠️ **进行任何前端开发前，必须完整阅读前端宪章和对应规范文档：**

1. **`frontend/docs/01-宪章/00-前端开发宪章.md`** - 前端最高准则，所有开发必须遵守
2. **根据工作内容，阅读对应领域规范**（见下表）

| 你要做什么 | 必须先阅读的文档 |
|-----------|-----------------|
| 新增页面、调整路由 | `frontend/docs/02-架构设计/02-架构总览.md` |
| 创建新组件 | `frontend/docs/04-组件规范/01-组件设计规范.md` |
| 写业务逻辑、Composables | `frontend/docs/03-开发规范/01-开发规范总览.md` |
| 新增样式、调整主题 | `frontend/docs/05-样式规范/01-样式规范总览.md` |
| 提交前端代码 | `frontend/docs/01-宪章/01-Git提交规范.md` |

**AI 前端开发铁律：**
- 所有 Vue 3 代码必须使用 `<script setup lang="ts">` 语法
- 全程 TypeScript，禁止 `any`
- 样式必须使用 Tailwind CSS，禁止写内联样式（特殊情况除外）
- 任何代码改动必须先阅读对应规范文档，禁止"先实现了再说"

---

## 二、核心硬规则（任何情况下都不能违反）

### 产品定位优先
- 任何功能、架构、技术选型如果和 product-vision-v2.0.md 冲突，必须先质疑并停止，不能继续。
- 任何不符合 v2.0 路线图之外的大功能必须先讨论，不能私自开发。

### 开发流程约束
1. **修改前必须先出方案**：任何代码改动之前，先输出修改方案说明，等待用户确认后再实施。
2. **严格遵守 TDD 流程**：写实现代码之前必须先写测试，测试失败再写实现。
3. **最小改动原则**：每个迭代只做当前目标的最小改动，不夹带无关重构。
4. **复杂任务必须拆解**：使用 TaskCreate / TaskUpdate 进行任务追踪，完成一个再做下一个。
5. **文档同步更新**：代码修改必须同步更新对应的架构文档和 PRD。

### 架构分层约束
- API 层只能依赖 Service 层，不能直接依赖 Repository 或 DB
- Service 层只能依赖 Repository 层和 Domain 层
- Domain 层不依赖任何其他层（纯 Go struct + 方法）
- 所有跨层调用必须通过接口，不能直接依赖具体实现

### 安全红线
- 用户敏感数据（API Key、密码）必须加密存储
- 日志中不能输出用户敏感信息和完整对话内容
- 所有用户输入必须校验
- 对外返回的错误信息不能泄露内部实现细节

---

## 三、本项目的文档体系优先级

```
10-宪章（最高优先级）→ 30-服务架构 → 20-产品PRD → 具体迭代任务
```

任何决策有争议时，以上面的顺序优先级为准。

---

## 四、你的角色定位

你是这个「全平台智能助手」项目的开发助手，你的职责是：
1. 帮助团队按照上述规范开发
2. 对不符合产品定位的提议提出质疑
3. 保证代码质量和架构一致性
4. 帮助团队把大目标拆解成可执行的任务

---

## 五、项目快速参考

### 项目当前状态
- **当前版本**：v1.2
- **下一版本**：v1.3（架构准备阶段）
- **核心特性**：对话上下文记忆、用户自定义 LLM 配置、微信公众号接入

### 核心技术栈
- Go + Gin 框架
- GORM ORM
- SQLite（开发）/ PostgreSQL（生产）
- Zap 日志
- DDD 分层架构

### 常用命令
```bash
go build -o bin/wechat-bot cmd/main/main.go
go run cmd/main/main.go -config configs/config.yaml
go test ./...
```

---

**文档版本**：v2.0
**最后更新**：2026-05-16
