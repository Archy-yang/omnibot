# 样式规范

## 概述

本文档定义 OmniBot 前端样式规范，包括设计系统、颜色体系、排版、间距、响应式设计等。

---

## 设计系统

### 设计参考

参考以下开源项目设计风格：
- **ChatGPT**：清晰、简洁、功能优先
- **Notion AI**：简洁、现代、以内容为中心
- **Claude**：专业、稳重、高可读性

---

## 颜色体系

### 主色调 (Primary)

| 色阶 | 色值 | 用途 |
|--------|--------|------|
| 50 | `#f0f4ff` | 极浅背景 |
| 100 | `#e0e7ff` | 悬停背景 |
| 200 | `#c7d2fe` | 边框、边框高亮 |
| 300 | `#a5b4fc` | 次要按钮 |
| 400 | `#818cf8` | 图标 |
| **500** | `#6366f1` | **主色、按钮、链接** |
| 600 | `#4f46e5` | 悬停按钮 |
| 700 | `#4338ca` | 点击按钮 |
| 800 | `#3730a3` | 深色背景文字 |
| 900 | `#312e81` | 深色背景 |

### 中性色 (Neutral)

| 色阶 | 色值 | 用途 |
|--------|--------|------|
| 50 | `#f9fafb` | 页面背景 |
| 100 | `#f3f4f6` | 卡片背景、悬浮背景 |
| 200 | `#e5e7eb` | 边框、分割线 |
| 300 | `#d1d5db` | 禁用状态边框 |
| 400 | `#9ca3af` | 次要文字、占位符 |
| 500 | `#6b7280` | 正文次要文字 |
| 600 | `#4b5563` | 正文主要文字 |
| 700 | `#374151` | 标题文字 |
| 800 | `#1f2937` | 深色背景文字 |
| 900 | `#111827` | 深色背景 |

### 语义色 (Semantic)

| 类型 | 色值 | 用途 |
|------|--------|------|
| Success | `#10b981` | 成功状态、正确答案 |
| Warning | `#f59e0b` | 警告状态、提示 |
| Error | `#ef4444` | 错误状态、删除 |
| Info | `#3b82f6` | 信息提示、链接 |

### Tailwind 配置示例：

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f4ff',
          100: '#e0e7ff',
          200: '#c7d2fe',
          300: '#a5b4fc',
          400: '#818cf8',
          500: '#6366f1',
          600: '#4f46e5',
          700: '#4338ca',
          800: '#3730a3',
          900: '#312e81',
        }
      }
    }
  }
}
```

---

## 排版体系

### 字体家族

```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
             'Helvetica Neue', Arial, sans-serif;
```

### 字号层级

| 层级 | 字号 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| H1 | 32px | 40px | 700 | 页面标题 |
| H2 | 24px | 32px | 600 | 次级标题 |
| H3 | 20px | 28px | 600 | 卡片标题 |
| Body-L | 18px | 28px | 400 | 正文大号 |
| Body | 16px | 24px | 400 | 正文常规 |
| Body-S | 14px | 20px | 400 | 辅助文字 |
| Caption | 12px | 16px | 400 | 说明文字 |
| Code | 14px | 20px | 400 | 代码、命令 |

### 字重

| 字重 | 数值 | 用途 |
|--------|------|------|
| Normal | 400 | 正文 |
| Medium | 500 | 强调文字、按钮 |
| Semibold | 600 | 标题、重要文字 |
| Bold | 700 | 大标题 |

---

## 间距体系

使用 4px 基础间距系统：

| 间距名 | 数值 | 用途 |
|---------|------|------|
| xs | 4px | 极小间距、图标内边距 |
| sm | 8px | 小组件内边距、紧凑布局 |
| md | 12px | 组件内边距 |
| base | 16px | 标准内边距、卡片间距 |
| lg | 24px | 大组件内边距、区块间距 |
| xl | 32px | 页面区块间距 |
| 2xl | 48px | 大区块间距 |
| 3xl | 64px | 页面边距 |

### 间距使用原则

- **水平间距**：按钮间距 8px，表单控件间距 16px
- **垂直间距**：段落间距 16px，区块间距 24px
- **内边距**：卡片内边距 16-24px，按钮内边距 8px 16px

---

## 圆角体系

| 圆角名 | 数值 | 用途 |
|---------|------|------|
| none | 0px | 尖角 |
| sm | 4px | 小组件、输入框 |
| md | 6px | 按钮、卡片 |
| lg | 8px | 大卡片、弹窗 |
| xl | 12px | 容器、面板 |
| full | 9999px | 圆形、胶囊按钮 |

---

## 阴影体系

| 阴影层级 | 用途 |
|-----------|------|
| shadow-sm | 输入框聚焦、小组件 |
| shadow | 卡片、按钮悬浮 |
| shadow-md | 悬浮卡片、弹窗 |
| shadow-lg | 模态框、抽屉 |

```css
/* 示例 */
.shadow-soft {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}
```

---

## 组件样式规范

### 按钮样式

| 类型 | 样式 |
|------|------|
| Primary | bg-primary-500 text-white hover:bg-primary-600 |
| Secondary | bg-gray-100 text-gray-700 hover:bg-gray-200 |
| Outline | border border-gray-200 bg-white hover:bg-gray-50 |
| Ghost | hover:bg-gray-100 text-gray-700 |

### 输入框样式

```css
.input-base {
  @apply px-4 py-2.5 border border-gray-200 rounded-lg;
  @apply text-gray-700 bg-white;
  @apply focus:outline-none focus:ring-2 focus:ring-primary-500/20 focus:border-primary-500;
  @apply transition-all duration-200;
}
```

### 消息气泡样式

```css
/* 用户消息 */
.message-user {
  @apply bg-primary-500 text-white rounded-2xl rounded-br-md;
}

/* 助手消息 */
.message-assistant {
  @apply bg-white text-gray-700 border border-gray-100;
  @apply rounded-2xl rounded-bl-md;
}
```

---

## 响应式设计

### 断点定义

| 断点 | 宽度 | 设备 |
|------|------|------|
| sm | 640px | 手机横屏 |
| md | 768px | 平板 |
| lg | 1024px | 小屏桌面 |
| xl | 1280px | 桌面 |
| 2xl | 1536px | 大屏桌面 |

### 移动端优先策略

```css
/* 默认样式（移动端） */
.container {
  padding: 16px;
}

/* 平板及以上 */
@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
}
```

---

## 动画规范

### 过渡时长

| 时长 | 用途 |
|------|------|
| 150ms | 颜色、阴影变化 |
| 200ms | 位移、缩放、普通过渡 |
| 300ms | 页面切换、弹窗动画 |

### 缓动函数

```css
transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
```

### 常用动画

```css
/* 淡入 */
.fade-enter-active {
  transition: opacity 0.2s ease;
}
.fade-enter-from {
  opacity: 0;
}

/* 滑入 */
.slide-up-enter-active {
  transition: all 0.3s ease;
}
.slide-up-enter-from {
  opacity: 0;
  transform: translateY(10px);
}
```

---

## 暗色模式 (Dark Mode)

### 暗色模式配色原则

1. 纯黑背景避免使用 `#000000`，使用 `#0f172a` 或 `#111827`
2. 文字对比度适中，避免纯白 `#ffffff`，使用 `#f9fafb`
3. 边框使用半透明白色或深灰色

### 暗色模式颜色映射

| 亮色 | 暗色 | 说明 |
|------|------|------|
| bg-white | bg-gray-900 | 卡片背景 |
| bg-gray-50 | bg-gray-800 | 页面背景 |
| border-gray-200 | border-gray-700 | 边框 |
| text-gray-700 | text-gray-200 | 正文 |
| text-gray-500 | text-gray-400 | 次要文字 |

### 实现方式

使用 Tailwind 的 `dark:` 变体 + CSS 变量：

```html
<!-- 支持深色模式 -->
<div class="bg-white dark:bg-gray-900 text-gray-700 dark:text-gray-200">
  内容
</div>
```

---

## 代码高亮样式

### 代码块设计

```css
.code-block {
  @apply bg-gray-900 text-gray-100 rounded-lg overflow-hidden;
  @apply font-mono text-sm;
}

.code-header {
  @apply flex items-center justify-between px-4 py-2;
  @apply bg-gray-800 border-b border-gray-700;
  @apply text-gray-400 text-xs;
}
```

---

**最后更新：** 2026-05-17
**版本：** v1.0
